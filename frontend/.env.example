# Base URL of the backend API. Vite's dev server also proxies /api to
# http://localhost:3000 (see vite.config.ts), so this is mainly needed
# for production builds pointed at a deployed backend.
VITE_API_BASE_URL=http://localhost:3000
